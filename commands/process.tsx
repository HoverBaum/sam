import type { CommandArgs, CommandContext } from "../types.ts";

export async function runProcessCommand(_context: CommandContext, _args: CommandArgs): Promise<void> {
  console.log("`sam process` is not implemented yet (planned for Phase 3).");
}

