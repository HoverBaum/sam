import type { CommandArgs, CommandContext } from "../types.ts";

export async function runNewCommand(_context: CommandContext, _args: CommandArgs): Promise<void> {
  console.log("`sam new` is not implemented yet (planned for Phase 1).");
}

